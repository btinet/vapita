{% extends 'admin.html.twig' %}

{% block title %}States{% endblock %}

{% block content %}
    <h1>States</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ state.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ state.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('states_index') }}">back to list</a>

    <a href="{{ path('states_edit', {'id': state.id}) }}">edit</a>

    {{ include('states/_delete_form.html.twig') }}
{% endblock %}
